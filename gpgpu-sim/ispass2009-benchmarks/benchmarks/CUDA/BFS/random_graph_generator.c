#include<stdio.h>
#include<stdlib.h>
int main( int argc, char** argv)
{
	int n_edges = 10, n_vertices = 5;
	char *file_name;
	if (argc == 3)
	{
		n_vertices = atoi(argv[0]);
		n_edges = atoi(argv[1]);
	}
	printf("%d\n", n_vertices);
	int i = 0;
	for(i=0; i < n_vertices;)
	{
		printf()
	}
				
}
